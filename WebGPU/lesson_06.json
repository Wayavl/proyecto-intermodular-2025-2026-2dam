{
  "language_name": "Español",
  "subject": "WebGPU",
  "name": "Lección 6: Bind Groups y Layouts",
  "content_md": "# Bind Groups y Layouts\n\nLos *bind groups* agrupan recursos (buffers, texturas, samplers) que los shaders pueden acceder. Un *layout* describe la estructura de esos recursos.\n\n## Creación de un BindGroupLayout\n```ts\nconst device = await navigator.gpu.requestAdapter().then(a => a.requestDevice());\nconst bindGroupLayout = device.createBindGroupLayout({\n  entries: [\n    {binding: 0, visibility: GPUShaderStage.VERTEX, buffer: {type: 'uniform'}},\n    {binding: 1, visibility: GPUShaderStage.FRAGMENT, sampler: {}},\n    {binding: 2, visibility: GPUShaderStage.FRAGMENT, texture: {}}\n  ]\n});\n```\n\n## Creación de un BindGroup usando ese layout\n```ts\nconst uniformBuffer = device.createBuffer({size: 64, usage: GPUBufferUsage.UNIFORM | GPUBufferUsage.COPY_DST});\nconst sampler = device.createSampler();\nconst texture = device.createTexture({size: {width: 256, height: 256, depthOrArrayLayers: 1}, format: 'rgba8unorm', usage: GPUTextureUsage.TEXTURE_BINDING});\n\nconst bindGroup = device.createBindGroup({\n  layout: bindGroupLayout,\n  entries: [\n    {binding: 0, resource: {buffer: uniformBuffer}},\n    {binding: 1, resource: sampler},\n    {binding: 2, resource: texture.createView()}\n  ]\n});\n```\n\n> **Tip**: Define siempre el layout antes del bind group; permite reutilizarlo en varios pipelines."
}
